from setuptools import setup, find_packages

setup(name='regla_channels_snapchat',
      version='0.0.1',
      description='Regla\'s Snapchat plugin library.',
      author='Xyla',
      author_email='gklei89@gmail.com',
      license='MIT',
      packages=find_packages(),
      install_requires=[
      ],
      zip_safe=False)
